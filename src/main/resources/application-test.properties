# Database properties for test
spring.datasource.url=jdbc:mysql://localhost:3306/vetcare?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=

# Disable Flyway for tests if schema management is not required for test cases
spring.flyway.enabled=false

# Use H2 (in-memory database) for tests (optional)
# spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driver-class-name=org.h2.Driver
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# JPA settings
spring.jpa.hibernate.ddl-auto=create-drop
